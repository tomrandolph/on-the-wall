[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
sqlx = { version = "0.8.6", features = ["postgres"] }
tokio = { version = "1.52.3", features = ["rt-multi-thread"] }
